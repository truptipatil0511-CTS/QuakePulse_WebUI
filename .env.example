# QuakePulse WebUI – Environment Configuration
# Copy relevant values into src/environments/environment.ts

# Azure Maps subscription key (get from Azure Portal → Azure Maps Account → Authentication)
AZURE_MAPS_KEY=your_azure_maps_key_here

# Backend API base URL
API_BASE_URL=http://localhost:5000
